package nl.mpcjanssen.simpletask.dao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import nl.mpcjanssen.simpletask.dao.TaskDaoConverter;
import nl.mpcjanssen.simpletask.task.Task;

import nl.mpcjanssen.simpletask.dao.gen.TodoListItem;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TODO_LIST_ITEM".
*/
public class TodoListItemDao extends AbstractDao<TodoListItem, Long> {

    public static final String TABLENAME = "TODO_LIST_ITEM";

    /**
     * Properties of entity TodoListItem.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Line = new Property(0, long.class, "line", true, "LINE");
        public final static Property Task = new Property(1, String.class, "task", false, "TASK");
        public final static Property Selected = new Property(2, boolean.class, "selected", false, "SELECTED");
    };

    private final TaskDaoConverter taskConverter = new TaskDaoConverter();

    public TodoListItemDao(DaoConfig config) {
        super(config);
    }
    
    public TodoListItemDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TODO_LIST_ITEM\" (" + //
                "\"LINE\" INTEGER PRIMARY KEY NOT NULL ," + // 0: line
                "\"TASK\" TEXT NOT NULL ," + // 1: task
                "\"SELECTED\" INTEGER NOT NULL );"); // 2: selected
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TODO_LIST_ITEM\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, TodoListItem entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getLine());
        stmt.bindString(2, taskConverter.convertToDatabaseValue(entity.getTask()));
        stmt.bindLong(3, entity.getSelected() ? 1L: 0L);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public TodoListItem readEntity(Cursor cursor, int offset) {
        TodoListItem entity = new TodoListItem( //
            cursor.getLong(offset + 0), // line
            taskConverter.convertToEntityProperty(cursor.getString(offset + 1)), // task
            cursor.getShort(offset + 2) != 0 // selected
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, TodoListItem entity, int offset) {
        entity.setLine(cursor.getLong(offset + 0));
        entity.setTask(taskConverter.convertToEntityProperty(cursor.getString(offset + 1)));
        entity.setSelected(cursor.getShort(offset + 2) != 0);
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(TodoListItem entity, long rowId) {
        entity.setLine(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(TodoListItem entity) {
        if(entity != null) {
            return entity.getLine();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
