name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: "Checkout"
      uses: actions/checkout@v1
    - name: "Run build"
      uses: msfjarvis/setup-android@1.0
      with:
        entrypoint: ./gradlew
        args: assembleCloudless
