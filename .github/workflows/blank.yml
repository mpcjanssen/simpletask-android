name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: "Checkout"
      uses: actions/checkout@v1
    - name: "Setup android"
      uses: msfjarvis/setup-android@1.0
    - name: "Run build"  
      run: ./gradlew assembleCloudless
